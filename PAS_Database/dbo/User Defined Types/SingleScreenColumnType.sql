CREATE TYPE [dbo].[SingleScreenColumnType] AS TABLE (
    [FieldName]      VARCHAR (MAX) NULL,
    [FieldValue]     VARCHAR (MAX) NULL,
    [FieldType]      VARCHAR (MAX) NULL,
    [ReferenceTable] VARCHAR (MAX) NULL);

